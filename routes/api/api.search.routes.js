const router = require('router').Router();
//Db

router.post('/', async (req, res) => {
  try {
    const { word } = req.body;
    const 
  } catch ({ message }) {
    res.json({ message });
  }
});

module.exports = router;
